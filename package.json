{
  "name": "common-install",
  "version": "1.0.0",
  "author": "ljlopes",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "ts-node-dev --trace-warnings --respawn ./src/index.ts",
    "test": "jest --coverage --verbose",
    "build": "tsc && npm run obfuscate",
    "obfuscate": "javascript-obfuscator dist --output target/release"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0"
  },
  "main": "index.ts",
  "dependencies": {
    "@ljlopes/common": "^1.4.0"
  }
}
